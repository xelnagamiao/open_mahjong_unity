# 工作区与 `other` 使用规则

本规则用于区分任务过程文件和需要交付给项目其他使用者的成果。

## 一、目录职责

### `.om_workspace/`：工作过程区

所有任务过程内容优先放在 `.om_workspace/<日期>-<任务名>/` 下。这里可以保存：

- 原始备份、待筛选素材、临时导出和中间版本；
- 一次性脚本、批处理脚本、实验代码和验证工具；
- 日志、JSON 报告、缓存、预览图、构建产物和失败输出；
- 已从项目或 `other` 移出的历史资料和可恢复归档。

`.om_workspace/` 已被 Git 忽略，只服务于当前机器上的工作过程，不作为项目交付入口。不得为了提交而使用 `git add -f` 强行加入其中的文件。

### `other/`：可交付成果区

`other/` 只存放已经整理完成、需要上传 Git 并提供给项目其他使用者的成果，例如：

- 最终美术资源、PSD、模型源文件和必要的源素材；
- 可复用且仍在维护的工具；
- 许可证、来源说明、使用说明和必要的验收报告。

不要把临时工作文件、示例工程、缓存、构建目录、运行日志、一次性 JSON、失败输出或未筛选的素材放入 `other/`。如果某项内容还没有决定是否交付，先放在 `.om_workspace/`。

## 二、处理流程

1. 先在 `.om_workspace/<日期>-<任务名>/` 中收集、备份、转换和验证。
2. 确认成果会被项目其他使用者复用后，再把最终版本复制到 `other/` 的清晰目录中。
3. `other/` 中的每个新资源目录应尽量附带 README，说明用途、来源、许可证、规格和修改方式。
4. 源文件与运行时文件分开保存；例如 PSD/Blender 源文件不与临时导出、日志和缓存混放。
5. 丢失的源文件不得用伪造的 PSD、假源文件或无法追溯的重建文件冒充；只能保留真实存在的源文件，并在 README 中说明缺失情况。
6. 需要进入主项目运行的代码、配置和 Unity 资源仍放在对应的 `open_mahjong_*` 项目目录中；`other/` 是共享成果和源素材区，不替代主项目入口。
7. 完成任务前检查 `git status`，确认 `other/` 只包含可交付内容，`.om_workspace/` 中的过程文件不会被提交。

## 三、提交边界

- 工作区中的备份和过程文件可以用于回滚，但不应上传 Git。
- `other/` 中的成果默认属于待提交范围；如果只想本机保留，应放回 `.om_workspace/`。
- 不自动提交或推送 Git；提交前由维护者审阅清单、许可证、来源和文件体积。
- 清理任务应优先采用可恢复移动，把被移出的内容放入 `.om_workspace/` 的隔离归档，而不是直接永久删除。

## 四、推荐结构

```text
.om_workspace/
└── 20260912-tablecloth-cleanup/
    ├── scratch/       # 临时脚本和中间产物
    ├── backup/        # 工作前备份
    ├── reports/       # 本机验证报告
    └── archive/       # 被清理但可恢复的历史资料

other/
└── Tablecloth/
    ├── README.md      # 共享说明、来源、许可证、规格
    ├── PSD/            # 真实可编辑源文件
    └── ...             # 最终运行时素材或可交付源素材
```
